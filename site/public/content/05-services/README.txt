05-SERVICES
===========
OPTIONAL per-service still or loop, shown next to that service's row.
Numeric prefix must match the service's position in lib/placeholders.ts
SERVICES (01 = Brand Motion Systems, 02 = Title Sequences & Broadcast
Packages, 03 = 3D & Compositing, 04 = Social / Vertical Motion,
05 = AI-Assisted Pipeline).

01-brand-motion-systems.mp4   16:9 or 1:1, <=6MB, no audio, loud loop-friendly.
01-brand-motion-systems.jpg   optional still instead of a loop.

Empty -> row renders with a gradient placeholder, text still shows.
