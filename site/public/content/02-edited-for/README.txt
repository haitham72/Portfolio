02-EDITED-FOR
=============
Client logo ticker. HIDE-WHEN-EMPTY — this whole section disappears if the
folder is empty, it does not render a placeholder ticker.

01-client-name.svg
02-client-name.png
...

Numeric prefix = ticker order. SVG preferred (grayscale -> colour on hover
needs a flat single-colour mark to invert cleanly). PNG needs a transparent
background.
